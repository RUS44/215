#include <iostream> 
using namespace std; 
int main() 
{ 
system ("chcp 1251 > nul"); 
float x,y; 
cout « "Введите координаты точек плоскости: "« '\n'; 
cout « "x= "; cin » x; cout « '\n'; 
cout « "y= "; cin » y; cout « '\n'; 
if ((y<=x)&&(y<=70)&&(x<=70)) 
{ 
cout « "Точка принадлежит заштрихованной области" « '\n' « "До свидания! Программа завершает работу."« '\n'« '\n'; 
} 
else 
{ 
cout « "Точка НЕ принадлежит заштрихованной области" « '\n' « "До свидания! Программа завершает работу."« '\n'« '\n'; 
} 
system ("PAUSE"); 
return 0; 

}
